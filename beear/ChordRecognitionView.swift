import SwiftUI

struct ChordRecognitionView: View {
    var body: some View {
        VStack {
            Text("Reconnaissance d'accords")
                .font(.title)
                .padding()
            
            // Ajoutez ici les éléments pour choisir la catégorie des accords et les reconnaître
        }
        .navigationTitle("Accords")
    }
}

#Preview {
    ChordRecognitionView()
}
