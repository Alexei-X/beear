import SwiftUI

struct NoteRecognitionView: View {
    var body: some View {
        VStack {
            Text("Reconnaissance des notes")
                .font(.title)
                .padding()
            
            // Ajoutez ici les éléments pour reconnaître les notes
        }
        .navigationTitle("Notes")
    }
}

#Preview {
    NoteRecognitionView()
}
