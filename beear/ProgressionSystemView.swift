import SwiftUI

struct ProgressionSystemView: View {
    var body: some View {
        VStack {
            Text("Système de progression")
                .font(.title)
                .padding()
            
            // Ajoutez ici les éléments pour gérer les différentes difficultés et la progression
        }
        .navigationTitle("Progression")
    }
}

#Preview {
    ProgressionSystemView()
}
